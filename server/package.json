{
  "name": "cascade-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/cli.ts",
    "build": "tsc && mkdir -p dist/runtime && cp src/runtime/*.ts dist/runtime/",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "esbuild": "^0.28.2",
    "express": "^5.2.1",
    "multer": "^2.3.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/multer": "^2.2.0",
    "@types/node": "^24.13.3",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3"
  }
}
